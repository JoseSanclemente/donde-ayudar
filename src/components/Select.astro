---
import {
  caretHtml,
  SELECT_FIELD,
  SELECT_FIELD_WRAP,
} from "@/scripts/ui/select";

interface Props {
  id: string;

  label: string;

  class?: string;
}

const { id, label, class: extra = "" } = Astro.props;
---

<span class={`${SELECT_FIELD_WRAP} ${extra}`}>
  <select id={id} aria-label={label} class={SELECT_FIELD}>
    <slot />
  </select>
  <Fragment set:html={caretHtml("field")} />
</span>
