---
import { NEW_DOT } from "@/components/ui/NewDot";

interface Props {
  id?: string;

  class?: string;
  hidden?: boolean;

  label?: string;
}

const { id, class: extra = "", hidden = false, label } = Astro.props;
---

<span
  id={id}
  hidden={hidden}
  aria-hidden={label ? undefined : "true"}
  class={`${NEW_DOT} ${extra}`.trim()}
>
  {label && <span class="sr-only">{label}</span>}
</span>
